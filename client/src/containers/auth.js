import React from 'react';
import AuthComponent from '../components/Auth';

class Auth extends React.Component {
    render() {
        return (
            <AuthComponent />
        )
    }
}

export default Auth;